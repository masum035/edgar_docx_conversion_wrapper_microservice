server:
  port: 8080
  tomcat:
    max-connections: 2000
    accept-count: 200
#  servlet:
#    context-path: /masum 

# for swagger      
springdoc:
  swagger-ui:
    path: swagger-ui.html
    
spring:
  zipkin:
    base-url: http://localhost:9411/
  data:
    mongodb:
      uri: mongodb+srv://masum-wsd:masum-wsd-db-password@mongo-cluster-of-masum.ba9aw.mongodb.net/
      database: journalDB
      auto-index-creation: true
  main:
    allow-circular-references: true
    
# for actuator
management:
  tracing:
    sampling:
      probability: 1.0
  endpoint:
    health:
      show-details: always
    metrics:
      enabled: true
  endpoints:
    web:
      base-path: /actuator
      exposure:
        include: health,prometheus,env,metrics,beans,loggers,info 
# for object storage
minio:
  url: http://localhost:9000
  access:
    key: nw3xUC6zDrPtCGojNZ2J
  secret:
    key: 1HVXPTqPurYSoWcIyWyU91dNKbiNo04tHtiDgIR2
  bucket:
    name: edgar-bucket
# for logging
logging:
  level:
    root: warn
    console:
      level: WARN
